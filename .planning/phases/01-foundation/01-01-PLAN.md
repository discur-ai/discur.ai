---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - astro.config.ts
  - tsconfig.json
  - src/styles/global.css
  - src/styles/katex-overrides.css
  - src/env.d.ts
  - public/favicon.svg
  - public/robots.txt
  - .prettierrc
autonomous: true
requirements:
  - FOUN-01
  - FOUN-04
  - FOUN-05

must_haves:
  truths:
    - "npm run build produces static HTML output in dist/"
    - "Tailwind utility classes resolve to CSS custom properties defined in @theme"
    - "KaTeX CSS is imported and overridden for dark theme colors"
    - "Three Google Fonts are configured via Astro Font API with CSS variables --font-display, --font-body, --font-mono"
    - "No tailwind.config.js or tailwind.config.ts exists"
  artifacts:
    - path: "astro.config.ts"
      provides: "Astro 6 project config with Font API and Tailwind vite plugin"
      contains: "fontProviders.google()"
    - path: "src/styles/global.css"
      provides: "Tailwind v4 @theme design tokens and base styles"
      contains: "--color-void: #0a0a0f"
    - path: "src/styles/katex-overrides.css"
      provides: "KaTeX color overrides for dark theme"
      contains: "var(--color-chalk)"
    - path: "package.json"
      provides: "Project dependencies"
      contains: "katex"
  key_links:
    - from: "astro.config.ts"
      to: "src/styles/global.css"
      via: "Tailwind vite plugin processes @theme tokens"
      pattern: "tailwindcss\\(\\)"
    - from: "src/styles/global.css"
      to: "katex/dist/katex.min.css"
      via: "CSS @import"
      pattern: "@import.*katex"
---

<objective>
Scaffold the Astro 6 project with all build dependencies, Tailwind v4 design tokens, KaTeX CSS integration, font configuration, and base styles.

Purpose: Establish the build pipeline and design system that every component and page depends on. Nothing renders without this.
Output: A buildable Astro project with `npm run dev` working, design tokens defined, KaTeX CSS loaded and themed for dark mode.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Astro 6 project with all dependencies</name>
  <files>package.json, astro.config.ts, tsconfig.json, src/env.d.ts, public/favicon.svg, public/robots.txt, .prettierrc</files>
  <read_first>
    .planning/phases/01-foundation/01-RESEARCH.md
  </read_first>
  <action>
    Create the Astro 6 project from scratch (do NOT use `npm create astro` -- create files manually for full control).

    1. Create `package.json` with these exact dependencies:
       - dependencies: `astro@^6.1`, `katex@^0.16`, `remark-math@^6.0`, `rehype-katex@^7.0`
       - devDependencies: `tailwindcss@^4.2`, `@tailwindcss/vite@^4.2`, `typescript@^6.0`, `prettier@^3.8`, `prettier-plugin-astro@^0.14`, `prettier-plugin-tailwindcss@^0.7`
       - scripts: `"dev": "astro dev"`, `"build": "astro build"`, `"preview": "astro preview"`, `"check": "astro check"`

    2. Create `astro.config.ts` per D-06, D-07, D-08, D-09, D-10 with:
       - Import `defineConfig` and `fontProviders` from `'astro/config'`
       - Import `tailwindcss` from `'@tailwindcss/vite'`
       - Import `remarkMath` from `'remark-math'`
       - Import `rehypeKatex` from `'rehype-katex'`
       - `site: 'https://discur.ai'`
       - `fonts` array with three entries:
         - Cinzel: `provider: fontProviders.google()`, `name: 'Cinzel'`, `cssVariable: '--font-display'`, `weights: [400, 700]`, `styles: ['normal']`
         - Cormorant Garamond: `provider: fontProviders.google()`, `name: 'Cormorant Garamond'`, `cssVariable: '--font-body'`, `weights: [400, 500, 600]`, `styles: ['normal', 'italic']`
         - JetBrains Mono: `provider: fontProviders.google()`, `name: 'JetBrains Mono'`, `cssVariable: '--font-mono'`, `weights: [400, 500]`, `styles: ['normal']`
       - `markdown: { remarkPlugins: [remarkMath], rehypePlugins: [rehypeKatex] }`
       - `vite: { plugins: [tailwindcss()] }`

    3. Create `tsconfig.json` with `"extends": "astro/tsconfigs/strict"`

    4. Create `src/env.d.ts` with `/// <reference types="astro/client" />`

    5. Create `public/favicon.svg` -- a simple SVG placeholder (a gold octagon or geometric shape on transparent background, using color #C4A35A per D-03)

    6. Create `public/robots.txt` with:
       ```
       User-agent: *
       Allow: /
       Sitemap: https://discur.ai/sitemap.xml
       ```

    7. Create `.prettierrc` with:
       ```json
       {
         "semi": true,
         "singleQuote": true,
         "trailingComma": "all",
         "plugins": ["prettier-plugin-astro", "prettier-plugin-tailwindcss"]
       }
       ```

    8. Run `npm install` to install all dependencies.

    ANTI-PATTERNS TO AVOID:
    - Do NOT use `npm create astro` (we need full control over config)
    - Do NOT create `tailwind.config.js` or `tailwind.config.ts` (Tailwind v4 uses CSS-first @theme)
    - Do NOT install `@astrojs/tailwind` (deprecated, installs v3)
  </action>
  <verify>
    <automated>cd /home/aescalera/code/github.com/discur-ai/discur.ai && node -e "const pkg = require('./package.json'); const deps = {...pkg.dependencies, ...pkg.devDependencies}; const required = ['astro','katex','tailwindcss','@tailwindcss/vite','prettier']; const missing = required.filter(r => !deps[r]); if(missing.length) { console.error('Missing:', missing); process.exit(1); } console.log('All dependencies present');" && test ! -f tailwind.config.js && test ! -f tailwind.config.ts && echo "No tailwind config file (correct)" && test -f astro.config.ts && echo "astro.config.ts exists"</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains "astro" in dependencies
    - package.json contains "katex" in dependencies
    - package.json contains "tailwindcss" in devDependencies
    - package.json contains "@tailwindcss/vite" in devDependencies
    - astro.config.ts contains `fontProviders.google()`
    - astro.config.ts contains `cssVariable: '--font-display'`
    - astro.config.ts contains `cssVariable: '--font-body'`
    - astro.config.ts contains `cssVariable: '--font-mono'`
    - astro.config.ts contains `tailwindcss()`
    - astro.config.ts contains `remarkMath`
    - astro.config.ts contains `rehypeKatex`
    - tsconfig.json contains `astro/tsconfigs/strict`
    - No file named tailwind.config.js or tailwind.config.ts exists
    - node_modules/ directory exists (npm install succeeded)
  </acceptance_criteria>
  <done>Astro 6 project initializes, npm install succeeds, astro.config.ts has Font API + Tailwind vite plugin + markdown math pipeline configured</done>
</task>

<task type="auto">
  <name>Task 2: Create global CSS with Tailwind v4 @theme design tokens and KaTeX overrides</name>
  <files>src/styles/global.css, src/styles/katex-overrides.css</files>
  <read_first>
    .planning/phases/01-foundation/01-RESEARCH.md
    .planning/phases/01-foundation/01-CONTEXT.md
  </read_first>
  <action>
    Create the design token system and KaTeX dark theme overrides per D-01 through D-05.

    1. Create `src/styles/global.css`:
       ```css
       @import "tailwindcss";
       @import "katex/dist/katex.min.css";
       @import "./katex-overrides.css";

       @theme inline {
         --font-display: var(--font-display);
         --font-body: var(--font-body);
         --font-mono: var(--font-mono);
       }

       @theme {
         --color-void: #0a0a0f;
         --color-obsidian: #111116;
         --color-onyx: #1a1a22;
         --color-sigil: #c4a35a;
         --color-sigil-dim: #7a6830;
         --color-arcanum: #6b4c9a;
         --color-arcanum-dim: #4a3570;
         --color-chalk: #e0dcd4;
         --color-chalk-muted: #8a8680;
         --color-glyph: #e8d5a3;
         --spacing-section: clamp(4rem, 10vw, 10rem);
       }

       html {
         scroll-behavior: smooth;
       }

       body {
         background-color: var(--color-void);
         color: var(--color-chalk);
         font-family: var(--font-body);
         -webkit-font-smoothing: antialiased;
         -moz-osx-font-smoothing: grayscale;
       }

       ::selection {
         background-color: var(--color-sigil);
         color: var(--color-void);
       }
       ```

    2. Create `src/styles/katex-overrides.css`:
       ```css
       .katex {
         color: var(--color-chalk);
         font-size: 1.1em;
       }

       .katex-display {
         color: var(--color-glyph);
       }

       .katex .mord,
       .katex .mrel,
       .katex .mbin,
       .katex .mopen,
       .katex .mclose {
         color: inherit;
       }
       ```

    Color values per locked decisions:
    - D-01: Background #0A0A0F (--color-void), never pure black
    - D-02: Body text #E0DCD4 (--color-chalk), never pure white
    - D-03: Gold accent #C4A35A (--color-sigil)
    - D-04: Muted violet #6B4C9A (--color-arcanum) for math notation highlighting
    - D-05: Illuminated manuscript / alchemical text feel

    CRITICAL: No hex value of #000000 or #FFFFFF may appear anywhere in these files.
  </action>
  <verify>
    <automated>cd /home/aescalera/code/github.com/discur-ai/discur.ai && grep -q "@import \"tailwindcss\"" src/styles/global.css && grep -q "katex/dist/katex.min.css" src/styles/global.css && grep -q "#0a0a0f" src/styles/global.css && grep -q "#e0dcd4" src/styles/global.css && grep -q "#c4a35a" src/styles/global.css && grep -q "#6b4c9a" src/styles/global.css && ! grep -qi "#000000\|#ffffff" src/styles/global.css src/styles/katex-overrides.css && grep -q "var(--color-chalk)" src/styles/katex-overrides.css && echo "All design tokens and KaTeX overrides verified"</automated>
  </verify>
  <acceptance_criteria>
    - src/styles/global.css contains `@import "tailwindcss"`
    - src/styles/global.css contains `@import "katex/dist/katex.min.css"`
    - src/styles/global.css contains `--color-void: #0a0a0f`
    - src/styles/global.css contains `--color-chalk: #e0dcd4`
    - src/styles/global.css contains `--color-sigil: #c4a35a`
    - src/styles/global.css contains `--color-arcanum: #6b4c9a`
    - src/styles/global.css contains `--spacing-section: clamp(4rem, 10vw, 10rem)`
    - src/styles/global.css does NOT contain #000000 or #ffffff
    - src/styles/katex-overrides.css contains `color: var(--color-chalk)`
    - src/styles/katex-overrides.css contains `color: var(--color-glyph)`
    - src/styles/katex-overrides.css does NOT contain #000000 or #ffffff
  </acceptance_criteria>
  <done>Design token system defined in @theme block with all palette colors, font variables registered, KaTeX overridden for dark theme, no pure black or white values anywhere</done>
</task>

</tasks>

<verification>
1. `npm run build` exits 0 (may need a minimal src/pages/index.astro placeholder -- create one with `<html><body>test</body></html>` if needed for the build to pass)
2. No `tailwind.config.js` or `tailwind.config.ts` exists
3. `grep -r "#000000\|#ffffff" src/` returns no matches
4. `astro.config.ts` references all three font CSS variables
</verification>

<success_criteria>
Astro project builds successfully. Design tokens, font config, and KaTeX CSS integration are in place. The build pipeline is ready for components.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
