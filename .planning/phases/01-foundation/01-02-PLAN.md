---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/layouts/BaseLayout.astro
  - src/components/Header.astro
  - src/components/Footer.astro
  - src/components/ui/Section.astro
autonomous: true
requirements:
  - FOUN-05
  - FOUN-06

must_haves:
  truths:
    - "BaseLayout wraps every page with html, head, body and loads all three fonts via Astro Font component"
    - "Header is fixed with logo/wordmark only, no navigation links"
    - "Footer shows minimal contact and paper link"
    - "Section component provides consistent max-width, responsive padding, and optional scroll anchor ID"
  artifacts:
    - path: "src/layouts/BaseLayout.astro"
      provides: "Page shell with Font components, meta tags, global CSS import"
      contains: "Font cssVariable"
    - path: "src/components/Header.astro"
      provides: "Fixed minimal header with wordmark"
      contains: "Discur"
    - path: "src/components/Footer.astro"
      provides: "Minimal footer with contact info"
      contains: "footer"
    - path: "src/components/ui/Section.astro"
      provides: "Reusable section wrapper with responsive spacing"
      contains: "py-section"
  key_links:
    - from: "src/layouts/BaseLayout.astro"
      to: "src/styles/global.css"
      via: "CSS import in frontmatter"
      pattern: "import.*global\\.css"
    - from: "src/layouts/BaseLayout.astro"
      to: "astro:assets"
      via: "Font component import"
      pattern: "import.*Font.*from.*astro:assets"
---

<objective>
Create the page layout shell and reusable structural UI components that every content section will use.

Purpose: Establish the visual skeleton -- header, footer, section rhythm, and the base layout that wires fonts, meta tags, and global styles together. Phase 2 content sections will compose these components.
Output: BaseLayout.astro, Header.astro, Footer.astro, Section.astro -- all structural primitives.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<interfaces>
<!-- Key types and contracts the executor needs from Plan 01 outputs. -->

From src/styles/global.css (created in Plan 01):
- Tailwind @theme tokens: bg-void, text-chalk, text-sigil, text-chalk-muted, bg-obsidian, bg-onyx, text-glyph, text-arcanum
- Font variables: --font-display (Cinzel), --font-body (Cormorant Garamond), --font-mono (JetBrains Mono)
- Spacing: py-section maps to clamp(4rem, 10vw, 10rem)
- Tailwind utility classes auto-generated from @theme: font-display, font-body, font-mono

From astro.config.ts (created in Plan 01):
- Font CSS variables: --font-display, --font-body, --font-mono
- Site URL: https://discur.ai
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create BaseLayout and Header components</name>
  <files>src/layouts/BaseLayout.astro, src/components/Header.astro</files>
  <read_first>
    src/styles/global.css
    astro.config.ts
    .planning/phases/01-foundation/01-RESEARCH.md
  </read_first>
  <action>
    Create the page layout and header per D-14, D-15, D-09.

    1. Create `src/layouts/BaseLayout.astro`:
       - Import `Font` from `'astro:assets'` in frontmatter
       - Import `'../styles/global.css'` in frontmatter
       - Import `Header` from `'../components/Header.astro'`
       - Import `Footer` from `'../components/Footer.astro'`
       - Props interface: `{ title?: string; description?: string }`
       - Default title: `'Discur -- Self-organizing memory from octonionic algebra'`
       - Default description: `'Discur builds self-organizing memory systems using octonionic algebra. The octonionic trie routes, stores, and self-monitors using the non-associative structure of the octonions.'`
       - Template structure:
         ```
         <!doctype html>
         <html lang="en">
           <head>
             <meta charset="utf-8" />
             <meta name="viewport" content="width=device-width, initial-scale=1" />
             <title>{title}</title>
             <meta name="description" content={description} />
             <Font cssVariable="--font-display" preload />
             <Font cssVariable="--font-body" preload />
             <Font cssVariable="--font-mono" />
             <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
             <slot name="head" />
           </head>
           <body class="min-h-screen bg-void text-chalk antialiased">
             <Header />
             <main>
               <slot />
             </main>
             <Footer />
           </body>
         </html>
         ```

    2. Create `src/components/Header.astro`:
       - Per D-14: Minimal fixed header with logo/wordmark only, NO navigation links
       - Fixed positioning at top of viewport
       - Backdrop blur for readability when content scrolls behind
       - The wordmark "DISCUR" in Cinzel (font-display) with letter-spacing
       - Use color-sigil for the wordmark text
       - Structure:
         ```
         <header class="fixed top-0 left-0 right-0 z-50 backdrop-blur-md bg-void/80 border-b border-sigil-dim/20">
           <div class="mx-auto max-w-6xl px-4 sm:px-6 lg:px-8 py-4">
             <span class="font-display text-sigil tracking-[0.2em] text-lg uppercase">Discur</span>
           </div>
         </header>
         ```

    ANTI-PATTERNS:
    - Do NOT add nav links (per D-14: wordmark only)
    - Do NOT use #000000 or #FFFFFF anywhere
    - Do NOT import KaTeX or any JS library in these structural components
  </action>
  <verify>
    <automated>cd /home/aescalera/code/github.com/discur-ai/discur.ai && grep -q "Font cssVariable" src/layouts/BaseLayout.astro && grep -q "import.*global.css" src/layouts/BaseLayout.astro && grep -q "font-display" src/components/Header.astro && grep -q "Discur" src/components/Header.astro && ! grep -q "<nav" src/components/Header.astro && echo "BaseLayout and Header verified"</automated>
  </verify>
  <acceptance_criteria>
    - src/layouts/BaseLayout.astro contains `import { Font } from 'astro:assets'`
    - src/layouts/BaseLayout.astro contains `import '../styles/global.css'`
    - src/layouts/BaseLayout.astro contains `Font cssVariable="--font-display" preload`
    - src/layouts/BaseLayout.astro contains `Font cssVariable="--font-body" preload`
    - src/layouts/BaseLayout.astro contains `Font cssVariable="--font-mono"`
    - src/layouts/BaseLayout.astro contains `<html lang="en">`
    - src/layouts/BaseLayout.astro contains `<slot />`
    - src/components/Header.astro contains `Discur`
    - src/components/Header.astro contains `font-display`
    - src/components/Header.astro contains `fixed`
    - src/components/Header.astro does NOT contain `<nav`
    - Neither file contains #000000 or #ffffff
  </acceptance_criteria>
  <done>BaseLayout provides page shell with Font preloading, meta tags, and global CSS. Header is fixed with wordmark only, no navigation. Both use design tokens from global.css.</done>
</task>

<task type="auto">
  <name>Task 2: Create Footer and Section components</name>
  <files>src/components/Footer.astro, src/components/ui/Section.astro</files>
  <read_first>
    src/styles/global.css
    src/layouts/BaseLayout.astro
    .planning/phases/01-foundation/01-CONTEXT.md
  </read_first>
  <action>
    Create the footer and reusable section wrapper per D-16, D-17.

    1. Create `src/components/Footer.astro`:
       - Per D-17: Minimal footer with contact info and paper link
       - Border top in sigil-dim color for visual separation
       - Content centered, max-width consistent with sections
       - Include placeholder email link: `mailto:contact@discur.ai`
       - Include placeholder paper link text: "Read the paper" (href="#" for now, updated in Phase 2)
       - Copyright line with current year
       - Use chalk-muted color for secondary text, sigil for links
       - Structure:
         ```
         <footer class="border-t border-sigil-dim/20 py-12 px-4 sm:px-6 lg:px-8">
           <div class="mx-auto max-w-4xl flex flex-col sm:flex-row items-center justify-between gap-4 text-sm text-chalk-muted">
             <span>&copy; {new Date().getFullYear()} Discur</span>
             <div class="flex gap-6">
               <a href="mailto:contact@discur.ai" class="hover:text-sigil transition-colors">Contact</a>
               <a href="#" class="hover:text-sigil transition-colors">Read the paper</a>
             </div>
           </div>
         </footer>
         ```

    2. Create `src/components/ui/Section.astro`:
       - Per D-16: Base section component with consistent spacing, max-width, responsive padding
       - Props interface: `{ id?: string; class?: string; fullWidth?: boolean }`
       - Default `fullWidth` to `false`
       - Use `py-section` for vertical spacing (maps to `clamp(4rem, 10vw, 10rem)` via @theme)
       - Responsive horizontal padding: `px-4 sm:px-6 lg:px-8`
       - Max width `max-w-4xl` centered when not fullWidth
       - Support class:list for merging custom classes
       - Structure:
         ```
         <section
           id={id}
           class:list={[
             'py-section px-4 sm:px-6 lg:px-8',
             !fullWidth && 'mx-auto max-w-4xl',
             className,
           ]}
         >
           <slot />
         </section>
         ```
  </action>
  <verify>
    <automated>cd /home/aescalera/code/github.com/discur-ai/discur.ai && grep -q "footer" src/components/Footer.astro && grep -q "contact@discur.ai" src/components/Footer.astro && grep -q "py-section" src/components/ui/Section.astro && grep -q "max-w-4xl" src/components/ui/Section.astro && grep -q "fullWidth" src/components/ui/Section.astro && echo "Footer and Section verified"</automated>
  </verify>
  <acceptance_criteria>
    - src/components/Footer.astro contains `<footer`
    - src/components/Footer.astro contains `contact@discur.ai`
    - src/components/Footer.astro contains `Read the paper`
    - src/components/Footer.astro contains `text-chalk-muted`
    - src/components/Footer.astro contains `hover:text-sigil`
    - src/components/ui/Section.astro contains `py-section`
    - src/components/ui/Section.astro contains `px-4 sm:px-6 lg:px-8`
    - src/components/ui/Section.astro contains `max-w-4xl`
    - src/components/ui/Section.astro contains `fullWidth`
    - src/components/ui/Section.astro contains `<slot />`
  </acceptance_criteria>
  <done>Footer shows contact email and paper link with muted styling. Section component provides consistent spacing, max-width, and responsive padding with fullWidth option.</done>
</task>

</tasks>

<verification>
1. All four component files exist and contain the expected structure
2. BaseLayout imports and renders Header and Footer
3. No navigation links in Header
4. Section uses py-section spacing token
5. No #000000 or #FFFFFF in any component
</verification>

<success_criteria>
The structural skeleton is complete: BaseLayout wires fonts + meta + global CSS, Header shows fixed wordmark, Footer has contact + paper link, Section provides consistent content containment. Phase 2 content sections compose these directly.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
