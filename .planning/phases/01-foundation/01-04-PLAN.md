---
phase: 01-foundation
plan: 04
type: execute
wave: 3
depends_on:
  - 01-02
  - 01-03
files_modified:
  - src/pages/index.astro
autonomous: false
requirements:
  - FOUN-01
  - FOUN-02
  - FOUN-03
  - FOUN-04
  - FOUN-05
  - FOUN-06

must_haves:
  truths:
    - "npm run build produces static HTML with zero client-side JS for content rendering"
    - "A LaTeX equation renders as static HTML in the build output with no client-side math processing"
    - "An equation container on a 375px viewport scrolls horizontally without breaking page layout"
    - "Body text appears as off-white on dark gray with no pure white or pure black"
    - "Fonts load with preload hints and the page is usable during font loading"
    - "The page is responsive from 375px to 1440px+"
  artifacts:
    - path: "src/pages/index.astro"
      provides: "Landing page demonstrating all foundation components"
      contains: "BaseLayout"
    - path: "dist/index.html"
      provides: "Built static HTML output"
      min_lines: 50
  key_links:
    - from: "src/pages/index.astro"
      to: "src/layouts/BaseLayout.astro"
      via: "Layout wrapper"
      pattern: "import.*BaseLayout"
    - from: "src/pages/index.astro"
      to: "src/components/math/Equation.astro"
      via: "Equation component usage"
      pattern: "import.*Equation"
    - from: "src/pages/index.astro"
      to: "src/data/equations.ts"
      via: "Equation data import"
      pattern: "import.*equations"
    - from: "src/pages/index.astro"
      to: "src/components/ui/Section.astro"
      via: "Section component usage"
      pattern: "import.*Section"
---

<objective>
Create the demo index page that wires all foundation components together, then build and verify the entire stack works end-to-end.

Purpose: This is the integration test for the entire phase. It proves that Astro builds, KaTeX renders, fonts preload, colors are correct, and equations handle mobile overflow. The page content is a scaffold that Phase 2 replaces with real sections.
Output: Working index.astro page, successful `npm run build`, human verification of visual output.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
@.planning/phases/01-foundation/01-03-SUMMARY.md
</context>

<interfaces>
<!-- Key contracts from Plans 01-03 that this plan wires together. -->

From src/layouts/BaseLayout.astro (Plan 02):
```typescript
interface Props {
  title?: string;
  description?: string;
}
// Slots: default (body content), "head" (extra head elements)
```

From src/components/math/Equation.astro (Plan 03):
```typescript
interface Props {
  latex: string;
  label?: string;
  ariaLabel?: string;
}
```

From src/components/math/InlineMath.astro (Plan 03):
```typescript
interface Props {
  latex: string;
  ariaLabel?: string;
}
```

From src/components/ui/Section.astro (Plan 02):
```typescript
interface Props {
  id?: string;
  class?: string;
  fullWidth?: boolean;
}
```

From src/components/Header.astro (Plan 02):
- Fixed header with "DISCUR" wordmark, no props

From src/components/Footer.astro (Plan 02):
- Footer with contact email and paper link, no props

From src/data/equations.ts (Plan 03):
```typescript
export const ASSOCIATOR: string;
export const COMPOSITION_ERROR: string;
export const COMPRESSION_EFFICIENCY: string;
export const SUBALGEBRA_CLEANLINESS: string;
export const PREDICTION_CONSISTENCY: string;
export const ASSOCIATOR_HEALTH: string;
export const ASSOCIATOR_NORM_S7: string;
```

Color tokens available as Tailwind classes:
- bg-void, bg-obsidian, bg-onyx
- text-chalk, text-chalk-muted, text-glyph
- text-sigil, text-sigil-dim
- text-arcanum, text-arcanum-dim
- font-display (Cinzel), font-body (Cormorant Garamond), font-mono (JetBrains Mono)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create demo index page and verify build</name>
  <files>src/pages/index.astro</files>
  <read_first>
    src/layouts/BaseLayout.astro
    src/components/math/Equation.astro
    src/components/math/InlineMath.astro
    src/components/ui/Section.astro
    src/data/equations.ts
    src/styles/global.css
  </read_first>
  <action>
    Create `src/pages/index.astro` that demonstrates every foundation component in a scaffold page.

    This page serves two purposes:
    1. Integration test -- proves the full stack works (Astro + Tailwind + KaTeX + Fonts)
    2. Scaffold -- Phase 2 replaces these placeholder sections with real content

    Structure:
    ```astro
    ---
    import BaseLayout from '../layouts/BaseLayout.astro';
    import Section from '../components/ui/Section.astro';
    import Equation from '../components/math/Equation.astro';
    import InlineMath from '../components/math/InlineMath.astro';
    import {
      ASSOCIATOR,
      ASSOCIATOR_NORM_S7,
      COMPOSITION_ERROR,
    } from '../data/equations';
    ---

    <BaseLayout>
      <!-- Hero placeholder -->
      <Section id="hero" class="pt-32">
        <h1 class="font-display text-4xl sm:text-5xl lg:text-6xl text-glyph tracking-wide">
          Self-organizing memory from octonionic algebra
        </h1>
        <p class="mt-6 text-lg sm:text-xl text-chalk-muted max-w-2xl">
          The octonionic trie routes, stores, and self-monitors using the non-associative structure of the octonions.
        </p>
      </Section>

      <!-- Math rendering demo -->
      <Section id="math-demo">
        <h2 class="font-display text-2xl sm:text-3xl text-sigil mb-8">
          Mathematical Foundation
        </h2>
        <p class="text-chalk mb-6">
          The associator <InlineMath latex="[a,b,c]" ariaLabel="associator of a, b, c" /> measures the failure of associativity in the octonions.
        </p>
        <Equation
          latex={ASSOCIATOR}
          label="1"
          ariaLabel="Associator definition: bracket a, b, c equals a b times c minus a times b c"
        />
        <p class="text-chalk mb-6">
          The expected associator norm on the unit sphere has a remarkable closed form:
        </p>
        <Equation
          latex={ASSOCIATOR_NORM_S7}
          label="2"
          ariaLabel="Expected associator norm on S7 equals 147456 over 42875 pi"
        />
        <p class="text-chalk mb-6">
          Composition errors are bounded by:
        </p>
        <Equation
          latex={COMPOSITION_ERROR}
          label="3"
          ariaLabel="Composition error bound epsilon of d"
        />
      </Section>

      <!-- Typography demo -->
      <Section id="typography-demo">
        <h2 class="font-display text-2xl sm:text-3xl text-sigil mb-8">
          Typography System
        </h2>
        <p class="text-chalk mb-4">
          Body text in Cormorant Garamond -- warm, scholarly, readable at length.
        </p>
        <p class="font-display text-glyph text-xl mb-4">
          Display headings in Cinzel -- classical authority.
        </p>
        <p class="font-mono text-chalk-muted mb-4">
          Code and notation in JetBrains Mono -- technical clarity.
        </p>
        <p class="text-sigil mb-4">
          Gold accent text for emphasis and interactive elements.
        </p>
        <p class="text-arcanum mb-4">
          Violet accent for mathematical notation highlighting.
        </p>
      </Section>

      <!-- Color palette demo -->
      <Section id="palette-demo">
        <h2 class="font-display text-2xl sm:text-3xl text-sigil mb-8">
          Color Palette
        </h2>
        <div class="grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-5 gap-4">
          <div class="rounded-lg p-4 bg-void border border-sigil-dim/30">
            <span class="text-xs font-mono text-chalk-muted">void</span>
            <span class="block text-xs font-mono text-chalk-muted">#0A0A0F</span>
          </div>
          <div class="rounded-lg p-4 bg-obsidian border border-sigil-dim/30">
            <span class="text-xs font-mono text-chalk-muted">obsidian</span>
            <span class="block text-xs font-mono text-chalk-muted">#111116</span>
          </div>
          <div class="rounded-lg p-4 bg-onyx border border-sigil-dim/30">
            <span class="text-xs font-mono text-chalk-muted">onyx</span>
            <span class="block text-xs font-mono text-chalk-muted">#1A1A22</span>
          </div>
          <div class="rounded-lg p-4 bg-void border border-sigil-dim/30">
            <span class="text-xs font-mono text-sigil">sigil</span>
            <span class="block text-xs font-mono text-sigil">#C4A35A</span>
          </div>
          <div class="rounded-lg p-4 bg-void border border-sigil-dim/30">
            <span class="text-xs font-mono text-arcanum">arcanum</span>
            <span class="block text-xs font-mono text-arcanum">#6B4C9A</span>
          </div>
        </div>
      </Section>
    </BaseLayout>
    ```

    After creating the file, run:
    1. `npm run build` -- must exit 0 with no errors
    2. Verify the built HTML in `dist/index.html`:
       - Contains KaTeX rendered HTML (look for `class="katex"`)
       - Contains NO `<script>` tags loading KaTeX JS
       - Contains `<link rel="preload"` for font files
       - Contains the design token CSS custom properties
    3. If build fails, diagnose and fix the issue (most likely: import path, missing dependency, or Astro Font API config issue)

    CRITICAL VERIFICATION:
    - `grep -c 'katex' dist/index.html` should return a positive number (rendered math elements)
    - `grep -c 'script.*katex' dist/index.html` should return 0 (no client-side KaTeX)
    - No #000000 or #FFFFFF in source files
  </action>
  <verify>
    <automated>cd /home/aescalera/code/github.com/discur-ai/discur.ai && npm run build 2>&1 && test -f dist/index.html && grep -q "katex" dist/index.html && echo "KaTeX HTML present in build output" && ! grep -qi "script.*katex\|katex.*script" dist/index.html && echo "No client-side KaTeX JS" && grep -q "BaseLayout" src/pages/index.astro && grep -q "Equation" src/pages/index.astro && grep -q "InlineMath" src/pages/index.astro && grep -q "Section" src/pages/index.astro && echo "All components used in index.astro"</automated>
  </verify>
  <acceptance_criteria>
    - src/pages/index.astro contains `import BaseLayout`
    - src/pages/index.astro contains `import Section`
    - src/pages/index.astro contains `import Equation`
    - src/pages/index.astro contains `import InlineMath`
    - src/pages/index.astro contains `import` from `'../data/equations'`
    - `npm run build` exits with code 0
    - dist/index.html exists after build
    - dist/index.html contains `katex` (rendered math HTML)
    - dist/index.html does NOT contain script tags loading KaTeX JavaScript
    - src/pages/index.astro does NOT contain #000000 or #ffffff
  </acceptance_criteria>
  <done>Index page builds successfully. KaTeX equations render as static HTML in build output. All foundation components (BaseLayout, Header, Footer, Section, Equation, InlineMath) are wired together and functional.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of foundation</name>
  <files>src/pages/index.astro</files>
  <action>
    Start the dev server with `npm run dev` and present the page at http://localhost:4321 for human visual verification. The human will check color palette, typography, math rendering, responsive layout, header/footer, and zero-JS compliance against the locked decisions D-01 through D-17 and requirements FOUN-01 through FOUN-06.
  </action>
  <verify>Human confirms visual output matches all foundation requirements by typing "approved"</verify>
  <done>Human has visually verified: dark palette avoids halation, fonts render correctly, equations display as math notation (not raw LaTeX), equations scroll on mobile, header is fixed with wordmark only, footer shows contact and paper link, no KaTeX JS loads in the browser.</done>
</task>

</tasks>

<verification>
Phase 1 success criteria from ROADMAP.md:
1. Running `npm run build` produces static HTML with zero client-side JS for content rendering
2. A LaTeX equation renders as static HTML in the build output with no client-side math processing
3. An equation container on a 375px viewport scrolls horizontally without breaking the page layout
4. Body text appears as off-white on dark gray (no pure white on pure black anywhere in the palette)
5. Fonts load with preload hints and the page is usable during font loading
</verification>

<success_criteria>
The entire Phase 1 foundation is verified: build pipeline works, equations render at build time, dark palette avoids halation, fonts preload correctly, layout is responsive, and a human has visually confirmed the result. Phase 2 can begin content authoring on this scaffold.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-04-SUMMARY.md`
</output>
