---
phase: 02-content-sections
plan: 05
type: execute
wave: 3
depends_on: ["02-03", "02-04"]
files_modified:
  - src/pages/index.astro
  - src/components/Footer.astro
autonomous: true
requirements:
  - CONT-01
  - CONT-09

must_haves:
  truths:
    - "The landing page renders all 7 sections in the correct order from hero to contact"
    - "Footer shows research@discur.ai and links to GitHub paper"
    - "The page builds to static HTML with zero client-side JavaScript"
    - "Section order matches D-02: Hero, Mechanism, Architecture, Invariants, Results, Comparison, Contact"
  artifacts:
    - path: "src/pages/index.astro"
      provides: "Complete landing page assembling all section components"
      min_lines: 30
    - path: "src/components/Footer.astro"
      provides: "Updated footer with correct email and paper link"
  key_links:
    - from: "src/pages/index.astro"
      to: "src/components/sections/*.astro"
      via: "import and render in sequence"
      pattern: "import Hero"
    - from: "src/pages/index.astro"
      to: "src/layouts/BaseLayout.astro"
      via: "page shell wrapper"
      pattern: "import BaseLayout"
    - from: "src/components/Footer.astro"
      to: "research@discur.ai"
      via: "mailto link"
      pattern: "research@discur.ai"
---

<objective>
Assemble the complete landing page by wiring all section components into index.astro and updating the Footer with correct contact information.

Purpose: This is the final integration step. All section components exist from Plans 02-04. This plan replaces the Phase 1 demo content in index.astro with the production page, and fixes the Footer email (contact@ to research@ per D-12).

Output: A complete, buildable landing page with all content sections in the correct order.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-content-sections/02-CONTEXT.md
@.planning/phases/02-content-sections/02-UI-SPEC.md
@src/pages/index.astro
@src/components/Footer.astro
@src/layouts/BaseLayout.astro
</context>

<interfaces>
<!-- All section components created in Plans 03-04 -->
From src/components/sections/Hero.astro: standalone, no props
From src/components/sections/Mechanism.astro: standalone, no props
From src/components/sections/Architecture.astro: standalone, no props
From src/components/sections/Invariants.astro: standalone, no props
From src/components/sections/Results.astro: standalone, no props
From src/components/sections/Comparison.astro: standalone, no props
From src/components/sections/Contact.astro: standalone, no props

From src/layouts/BaseLayout.astro:
```typescript
interface Props { title?: string; description?: string; }
```

From src/components/Footer.astro: standalone, no props (needs email update)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Wire index.astro with all section components</name>
  <files>src/pages/index.astro</files>
  <read_first>
    - src/pages/index.astro (current demo content to be replaced)
    - src/layouts/BaseLayout.astro
    - .planning/phases/02-content-sections/02-CONTEXT.md (D-02 section order)
    - .planning/phases/02-content-sections/02-UI-SPEC.md (Page Flow table)
  </read_first>
  <action>
Replace the entire content of src/pages/index.astro. The demo sections (hero placeholder, math-demo, typography-demo, palette-demo) are replaced with the production section components.

EXACT file content:

```astro
---
import BaseLayout from '../layouts/BaseLayout.astro';
import Hero from '../components/sections/Hero.astro';
import Mechanism from '../components/sections/Mechanism.astro';
import Architecture from '../components/sections/Architecture.astro';
import Invariants from '../components/sections/Invariants.astro';
import Results from '../components/sections/Results.astro';
import Comparison from '../components/sections/Comparison.astro';
import Contact from '../components/sections/Contact.astro';
---

<BaseLayout
  title="Discur — Self-Organizing Memory from Octonionic Algebra"
  description="The octonionic trie is a self-organizing hierarchical memory that replaces gradient descent with algebraic operations derived from the octonions."
>
  <Hero />
  <Mechanism />
  <Architecture />
  <Invariants />
  <Results />
  <Comparison />
  <Contact />
</BaseLayout>
```

Section order per D-02: Hero > Mechanism (What It Does) > Architecture (How It Works) > Invariants (Structural Invariants) > Results > Comparison > Contact.

The BaseLayout already includes Header and Footer, so those do not need to be imported here. The title and description props are set for SEO (preparation for Phase 4 DEPL-02).
  </action>
  <verify>
    <automated>cd /home/aescalera/code/github.com/discur-ai/discur.ai && npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep "import Hero" src/pages/index.astro returns a match
    - grep "import Mechanism" src/pages/index.astro returns a match
    - grep "import Architecture" src/pages/index.astro returns a match
    - grep "import Invariants" src/pages/index.astro returns a match
    - grep "import Results" src/pages/index.astro returns a match
    - grep "import Comparison" src/pages/index.astro returns a match
    - grep "import Contact" src/pages/index.astro returns a match
    - grep "<Hero" src/pages/index.astro returns a match (component rendered)
    - grep "Self-Organizing Memory" src/pages/index.astro returns a match (page title)
    - The file does NOT contain "math-demo" or "typography-demo" or "palette-demo" (old demo content removed)
    - grep -c "math-demo\|typography-demo\|palette-demo" src/pages/index.astro returns 0
    - npm run build succeeds with exit code 0
  </acceptance_criteria>
  <done>index.astro assembles all 7 section components in correct D-02 order within BaseLayout. Build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Update Footer.astro with correct contact info and paper link</name>
  <files>src/components/Footer.astro</files>
  <read_first>
    - src/components/Footer.astro (current content with wrong email)
    - .planning/phases/02-content-sections/02-CONTEXT.md (D-11, D-12)
  </read_first>
  <action>
Update Footer.astro to change the email from contact@discur.ai to research@discur.ai (per D-12), and update the paper link from "#" to "https://github.com/discur-ai" (per D-11).

EXACT file content:

```astro
<footer class="border-t border-sigil-dim/20 py-12 px-4 sm:px-6 lg:px-8">
  <div class="mx-auto max-w-4xl flex flex-col sm:flex-row items-center justify-between gap-4 text-sm text-chalk-muted">
    <span>&copy; {new Date().getFullYear()} Discur</span>
    <div class="flex gap-6">
      <a href="mailto:research@discur.ai" class="hover:text-sigil transition-colors">research@discur.ai</a>
      <a href="https://github.com/discur-ai" class="hover:text-sigil transition-colors">Read the paper</a>
    </div>
  </div>
</footer>
```

Changes from current:
1. Email: `contact@discur.ai` -> `research@discur.ai` (both href and display text)
2. Paper link: `href="#"` -> `href="https://github.com/discur-ai"`
3. Contact link text: `Contact` -> `research@discur.ai` (show the actual email address)
  </action>
  <verify>
    <automated>cd /home/aescalera/code/github.com/discur-ai/discur.ai && grep "research@discur.ai" src/components/Footer.astro && echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - grep "research@discur.ai" src/components/Footer.astro returns a match
    - grep "github.com/discur-ai" src/components/Footer.astro returns a match
    - grep "contact@discur.ai" src/components/Footer.astro returns NO match (old email removed)
    - grep -c "contact@discur.ai" src/components/Footer.astro returns 0
    - grep 'href="#"' src/components/Footer.astro returns NO match (placeholder removed)
  </acceptance_criteria>
  <done>Footer shows research@discur.ai email and links to GitHub for the paper. Old contact@discur.ai and placeholder href="#" are removed.</done>
</task>

</tasks>

<verification>
- `npm run build` completes successfully with zero errors
- The built HTML contains all 7 sections in order
- Footer shows research@discur.ai (not contact@)
- Footer paper link points to github.com/discur-ai (not #)
- No client-side JavaScript in the build output (static HTML only)
- Page title contains "Discur" and "Octonionic"
</verification>

<success_criteria>
- index.astro renders all 7 sections in D-02 order
- Footer has correct email and paper link
- `npm run build` produces static HTML
- No demo content remains from Phase 1
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-sections/02-05-SUMMARY.md`
</output>
