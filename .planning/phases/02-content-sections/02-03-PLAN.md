---
phase: 02-content-sections
plan: 03
type: execute
wave: 2
depends_on: ["02-01", "02-02"]
files_modified:
  - src/components/sections/Hero.astro
  - src/components/sections/Mechanism.astro
  - src/components/sections/Architecture.astro
autonomous: true
requirements:
  - CONT-01
  - CONT-02
  - CONT-03
  - CONT-10

must_haves:
  truths:
    - "Hero section states what the octonionic trie is in one sentence with the Fano plane beside it"
    - "Mechanism section presents five operations as consequences of octonionic algebra"
    - "Architecture section explains octonions, the associator, and shows the trie diagram"
    - "All copy passes anti-slop checklist (zero em dashes, zero superlatives, zero hedge phrases)"
  artifacts:
    - path: "src/components/sections/Hero.astro"
      provides: "Hero section with headline, paragraph, and Fano plane"
      min_lines: 20
    - path: "src/components/sections/Mechanism.astro"
      provides: "Five mechanisms section with paragraphs and inline math"
      min_lines: 40
    - path: "src/components/sections/Architecture.astro"
      provides: "Architecture section with octonion primer, associator, and trie diagram"
      min_lines: 60
  key_links:
    - from: "src/components/sections/Hero.astro"
      to: "src/components/diagrams/FanoPlane.astro"
      via: "import and render side-by-side"
      pattern: "import FanoPlane"
    - from: "src/components/sections/Architecture.astro"
      to: "src/components/diagrams/TrieStructure.astro"
      via: "import and render full-width"
      pattern: "import TrieStructure"
    - from: "src/components/sections/Architecture.astro"
      to: "src/data/equations.ts"
      via: "import ASSOCIATOR constant"
      pattern: "import.*ASSOCIATOR"
---

<objective>
Build the top three content sections of the landing page: Hero, Mechanism, and Architecture. All copy is pre-written in this plan.

Purpose: These three sections form the top-of-page narrative arc. Hero states the identity (CONT-01). Mechanism shows the five operations (CONT-03). Architecture explains the mathematical foundation with the associator equation and trie diagram (CONT-02 partial). All copy must pass anti-slop checklist (CONT-10).

Output: Three Astro section components ready for assembly in index.astro.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-content-sections/02-CONTEXT.md
@.planning/phases/02-content-sections/02-RESEARCH.md
@.planning/phases/02-content-sections/02-UI-SPEC.md
@src/components/ui/Section.astro
@src/components/math/Equation.astro
@src/components/math/InlineMath.astro
@src/components/diagrams/FanoPlane.astro
@src/components/diagrams/TrieStructure.astro
@src/data/equations.ts
</context>

<interfaces>
<!-- Components available for import -->
From src/components/ui/Section.astro:
```typescript
interface Props { id?: string; class?: string; fullWidth?: boolean; }
```

From src/components/math/Equation.astro:
```typescript
interface Props { latex: string; label?: string; ariaLabel?: string; }
```

From src/components/math/InlineMath.astro:
```typescript
interface Props { latex: string; ariaLabel?: string; }
```

From src/components/diagrams/FanoPlane.astro:
```typescript
interface Props { class?: string; }
```

From src/components/diagrams/TrieStructure.astro:
```typescript
interface Props { class?: string; }
```

From src/data/equations.ts:
```typescript
export const ASSOCIATOR: string;
export const COMPOSITION_ERROR: string;
export const ASSOCIATOR_NORM_S7: string;
```

<!-- Layout contracts from UI-SPEC -->
Hero: text left (60%), Fano right (40%), stacks on mobile
Section headings: single word, Cinzel, sigil color, uppercase tracking
Body text: text-chalk, Cormorant Garamond
Section separator: border-t border-sigil-dim/20 on each section after hero
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create Hero.astro and Mechanism.astro section components</name>
  <files>src/components/sections/Hero.astro, src/components/sections/Mechanism.astro</files>
  <read_first>
    - src/components/ui/Section.astro
    - src/components/math/InlineMath.astro
    - src/components/diagrams/FanoPlane.astro
    - .planning/phases/02-content-sections/02-UI-SPEC.md (Hero Layout, Component Inventory)
    - .planning/phases/02-content-sections/02-CONTEXT.md (D-01 through D-05, D-16, D-17, D-22, D-24)
    - .planning/phases/02-content-sections/02-RESEARCH.md (Anti-Slop checklist, Anthropic style)
  </read_first>
  <action>
Create the directory src/components/sections/ if it does not exist.

**Hero.astro** - Per D-03 (architecture-first framing), D-04 (Fano plane side-by-side), D-29 (Fano as hero element).

Imports: Section, FanoPlane.

Layout: CSS grid on sm+ (text 60%, Fano 40%). Stacks on mobile (text first, Fano below centered at max-w-[280px]).

EXACT COPY to use (every character must appear as written):

Headline (h1):
```
A self-organizing memory that replaces gradient descent with octonionic algebra
```

Paragraph (p, 2 sentences):
```
The octonionic trie is a tree whose nodes are individual octonions. It routes, stores, verifies, and restructures using five operations that emerge from a single algebraic substrate.
```

Tailwind classes per UI-SPEC:
- h1: `font-display text-4xl sm:text-5xl lg:text-6xl text-glyph tracking-wide leading-tight`
- p: `mt-6 text-lg text-chalk max-w-xl`
- Grid container: `grid sm:grid-cols-[3fr_2fr] gap-8 sm:gap-12 items-center`
- Fano container (mobile): `flex justify-center sm:justify-end`
- Fano component: `class="w-full max-w-[280px] sm:max-w-[400px]"`

Section props: `id="hero" class="pt-32"`

No border-t on hero (it is the first section).

---

**Mechanism.astro** - Per D-05 (each mechanism gets 3-4 sentences), D-24 (heading: single word), D-22 (economy of mechanism shown through structure, not stated).

Imports: Section, InlineMath.

EXACT COPY to use:

Section heading (h2):
```
Mechanism
```

Five mechanism paragraphs. Each is a `<div>` with an h3 and paragraph. The h3 is the mechanism name in sigil color, the paragraph is in chalk.

Mechanism 1 heading: `Routing`
Mechanism 1 paragraph:
```
Each node in the trie decomposes incoming data along the seven quaternionic subalgebras of the octonions, indexed by the Fano plane. The child whose subalgebra captures the strongest projection receives the input. Routing requires no learned parameters: the subalgebra structure is a property of octonionic multiplication itself.
```

Mechanism 2 heading: `Novelty detection`
Mechanism 2 paragraph (with inline math):
```
The associator [a,b,c] = (ab)c - a(bc) measures whether a triple of octonions lies within a common quaternionic subalgebra. When an input produces small associator norms with existing children, it is algebraically compatible. When the norm is large for all children, the input represents genuinely novel structure and triggers a branching event.
```
(Use InlineMath for `[a,b,c] = (ab)c - a(bc)`)

Mechanism 3 heading: `Content update`
Mechanism 3 paragraph:
```
Nodes accumulate information through octonionic composition. The update rule composes the existing node content with new input via multiplication. Because octonionic multiplication is norm-preserving, sequential compositions neither explode nor vanish, and any composition can be reversed through algebraic inversion.
```

Mechanism 4 heading: `Consistency verification`
Mechanism 4 paragraph:
```
Before committing an update, the trie checks whether the change is consistent with prior predictions stored in the node's memory buffer. It computes the counterfactual: given the proposed update, how would past predictions have differed? If the discrepancy exceeds a threshold, the update is rejected and escalated to a branching decision. This reasoning is enabled by algebraic invertibility.
```

Mechanism 5 heading: `Structural health`
Mechanism 5 paragraph:
```
Five invariants derived from the algebra monitor the trie's structural integrity: composition error bounds limit depth, compression efficiency flags redundant subtrees, subalgebra cleanliness measures branch separation, prediction consistency tracks update stability, and associator health detects routing strain. These replace the role of loss functions in gradient-trained systems.
```

Layout: Section with `id="mechanism" class="border-t border-sigil-dim/20"`.
- h2: `font-display text-2xl sm:text-3xl text-sigil mb-12 tracking-wider uppercase`
- Each mechanism div: `mb-10 last:mb-0`
- h3: `font-display text-lg text-sigil mb-3`
- p: `text-chalk leading-relaxed`
  </action>
  <verify>
    <automated>cd /home/aescalera/code/github.com/discur-ai/discur.ai && test -f src/components/sections/Hero.astro && test -f src/components/sections/Mechanism.astro && echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - File src/components/sections/Hero.astro exists
    - File src/components/sections/Mechanism.astro exists
    - grep "A self-organizing memory" src/components/sections/Hero.astro returns a match (headline text)
    - grep "replaces gradient descent" src/components/sections/Hero.astro returns a match
    - grep "import FanoPlane" src/components/sections/Hero.astro returns a match
    - grep "grid" src/components/sections/Hero.astro returns a match (side-by-side layout)
    - grep "Routing" src/components/sections/Mechanism.astro returns a match
    - grep "Novelty detection" src/components/sections/Mechanism.astro returns a match
    - grep "Content update" src/components/sections/Mechanism.astro returns a match
    - grep "Consistency verification" src/components/sections/Mechanism.astro returns a match
    - grep "Structural health" src/components/sections/Mechanism.astro returns a match
    - grep "import InlineMath" src/components/sections/Mechanism.astro returns a match
    - Anti-slop: grep -c "\\-\\-" src/components/sections/Hero.astro returns 0 (no em dashes in copy)
    - Anti-slop: grep -c "\\-\\-" src/components/sections/Mechanism.astro returns 0 (no em dashes in copy; note: Astro component syntax may have --, count only within paragraph text)
    - Anti-slop: grep -ic "groundbreaking\|revolutionary\|paradigm" src/components/sections/Mechanism.astro returns 0
    - Anti-slop: grep -ic "it's worth noting\|interestingly\|importantly" src/components/sections/Mechanism.astro returns 0
    - grep "border-t border-sigil-dim" src/components/sections/Mechanism.astro returns a match (section separator)
  </acceptance_criteria>
  <done>Hero.astro displays headline with Fano plane side-by-side. Mechanism.astro presents five mechanisms with descriptive paragraphs. All copy matches the pre-written text exactly with zero anti-slop violations.</done>
</task>

<task type="auto">
  <name>Task 2: Create Architecture.astro section component</name>
  <files>src/components/sections/Architecture.astro</files>
  <read_first>
    - src/components/ui/Section.astro
    - src/components/math/Equation.astro
    - src/components/math/InlineMath.astro
    - src/components/diagrams/TrieStructure.astro
    - src/data/equations.ts (the corrected version from Plan 01)
    - .planning/phases/02-content-sections/02-UI-SPEC.md (Architecture Section layout)
    - .planning/phases/02-content-sections/02-CONTEXT.md (D-06, D-07, D-09, D-21)
    - oct-trie.pdf (pages 3-6 for Sections 2-3)
  </read_first>
  <action>
Create Architecture.astro. Per D-06: three-beat structure (intuition, formal definition, computational interpretation). Per D-21: brief primer on octonions. Per D-07: trie diagram as full-width hero element.

Imports: Section, Equation, InlineMath, TrieStructure, ASSOCIATOR from equations.ts.

EXACT COPY to use:

Section heading (h2):
```
Architecture
```

**Beat 1: Octonion primer (D-21)**

Paragraph 1:
```
The octonions are an eight-dimensional number system extending the quaternions. Like the quaternions, they are non-commutative. Unlike the quaternions, they are also non-associative: the expression (ab)c does not always equal a(bc). This failure of associativity, far from being a deficiency, provides the octonionic trie with its core computational signal.
```

Paragraph 2:
```
The octonions contain exactly seven quaternionic subalgebras, each a four-dimensional subspace where multiplication is associative. These subalgebras are indexed by the lines of the Fano plane, a finite projective geometry with seven points and seven lines. Each imaginary basis unit belongs to exactly three subalgebras, creating an overlapping structure that the trie uses as its routing channels.
```

**Beat 2: The associator (formal definition)**

Paragraph 3:
```
The associator is the mathematical object at the center of the octonionic trie. For three octonions a, b, and c, it measures how far their product deviates from associativity:
```

Then render: `<Equation latex={ASSOCIATOR} label="1" ariaLabel="Associator definition: bracket a, b, c equals the quantity a b times c minus a times b c" />`

Paragraph 4:
```
The associator vanishes when all three arguments lie in a common quaternionic subalgebra. It is nonzero precisely when the triple spans multiple subalgebras. For unit octonions, the associator norm is bounded between 0 and 2, with an expected value of approximately 1.09 for uniformly random inputs on the 7-sphere.
```

**Beat 3: Computational interpretation**

Paragraph 5:
```
In the trie, the associator answers a routing question: given a new input x, an existing child c, and their parent node n, does x belong in the same branch as c? The answer is graded, not binary.
```

Paragraph 6 (three cases, presented as a short paragraph list with colons per D-26):
```
A small associator norm indicates algebraic compatibility: x and c share a subalgebra in the context of n, and x is routed to c for composition. A large norm across all children signals novelty: x is algebraically incompatible with every existing branch and triggers the creation of a new node. Intermediate values represent partial compatibility, where the composition proceeds but the consistency check examines whether the update destabilizes prior predictions.
```

**Trie diagram section**

Paragraph 7 (before the diagram):
```
The resulting structure is a dynamically growing tree. Each node stores a single octonion. Edges are labeled with the quaternionic subalgebra that the child most strongly activates. The branching factor is bounded by seven (the number of Fano plane lines), and the depth is limited by composition error accumulation.
```

Then render the TrieStructure component inside a `<Section fullWidth>` wrapper or a full-width div with `mx-auto max-w-[800px]`. The trie diagram should be centered and responsive.

**Layout structure:**
- Use one `<Section id="architecture" class="border-t border-sigil-dim/20">` for the text content.
- For the trie diagram, use a div with `class="mt-12 mx-auto max-w-[800px] px-4"` within the section, OR use a separate Section with fullWidth. Choose whichever keeps the text at max-w-4xl and the diagram at max-w-[800px].

Tailwind classes:
- h2: `font-display text-2xl sm:text-3xl text-sigil mb-12 tracking-wider uppercase`
- Paragraphs: `text-chalk leading-relaxed mb-6`
- Equation: standard Equation component (already styled)
  </action>
  <verify>
    <automated>cd /home/aescalera/code/github.com/discur-ai/discur.ai && test -f src/components/sections/Architecture.astro && grep -c "import" src/components/sections/Architecture.astro</automated>
  </verify>
  <acceptance_criteria>
    - File src/components/sections/Architecture.astro exists
    - grep "import.*ASSOCIATOR" src/components/sections/Architecture.astro returns a match
    - grep "import Equation" src/components/sections/Architecture.astro returns a match
    - grep "import TrieStructure" src/components/sections/Architecture.astro returns a match
    - grep "eight-dimensional number system" src/components/sections/Architecture.astro returns a match (octonion primer)
    - grep "seven quaternionic subalgebras" src/components/sections/Architecture.astro returns a match
    - grep "Fano plane" src/components/sections/Architecture.astro returns a match
    - grep "measures how far" src/components/sections/Architecture.astro returns a match (associator explanation)
    - grep "routing question" src/components/sections/Architecture.astro returns a match (computational interpretation)
    - grep "algebraic compatibility" src/components/sections/Architecture.astro returns a match
    - grep "dynamically growing tree" src/components/sections/Architecture.astro returns a match (trie intro)
    - grep 'latex={ASSOCIATOR}' src/components/sections/Architecture.astro returns a match (equation rendered)
    - grep "border-t border-sigil-dim" src/components/sections/Architecture.astro returns a match
    - Anti-slop: grep -cP "\x{2014}" src/components/sections/Architecture.astro returns 0 (no em dashes)
    - Anti-slop: grep -ic "delve\|utilize\|leverage\|harness\|foster\|robust" src/components/sections/Architecture.astro returns 0
    - Anti-slop: grep -ic "tapestry\|odyssey\|symphony\|nexus\|pinnacle\|landscape\|journey" src/components/sections/Architecture.astro returns 0
  </acceptance_criteria>
  <done>Architecture.astro presents the three-beat structure (octonion primer, associator definition with rendered equation, computational interpretation) followed by the trie diagram. All copy matches pre-written text exactly with zero anti-slop violations.</done>
</task>

</tasks>

<verification>
- Hero headline matches D-03 framing exactly
- Fano plane renders side-by-side with hero text (D-04)
- Five mechanisms each have 3-4 sentence paragraphs (D-05)
- Architecture section has three-beat structure (D-06)
- Octonion primer present before associator (D-21)
- Trie diagram rendered full-width in architecture section (D-07)
- All section headings are single words (D-24)
- No "we" constructions anywhere (D-16)
- Zero em dashes in all copy (D-17 H5)
- Zero superlatives (D-17 H3)
- Zero hedge phrases (D-17 H4)
- No binary contrasts (D-17 H1)
- `npx astro check` passes
</verification>

<success_criteria>
- Three section components exist and import their dependencies correctly
- All pre-written copy appears verbatim in the components
- Anti-slop checklist passes on all copy text
- Components follow established Astro patterns (Section wrapper, Equation/InlineMath for math)
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-sections/02-03-SUMMARY.md`
</output>
