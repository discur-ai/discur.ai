---
phase: 02-content-sections
plan: 04
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/components/sections/Invariants.astro
  - src/components/sections/Results.astro
  - src/components/sections/Comparison.astro
  - src/components/sections/Contact.astro
autonomous: true
requirements:
  - CONT-02
  - CONT-06
  - CONT-07
  - CONT-08
  - CONT-09
  - CONT-10

must_haves:
  truths:
    - "Five structural invariants are each presented with their equation and explanation"
    - "Results section shows 95.2% MNIST accuracy and 0% catastrophic forgetting with proper context"
    - "Comparison table shows 3 rows across 4 architectures with gold-highlighted Oct-Trie column"
    - "Contact section provides research@discur.ai and github.com/discur-ai links"
    - "All copy passes anti-slop checklist"
  artifacts:
    - path: "src/components/sections/Invariants.astro"
      provides: "Five structural invariants with equations"
      min_lines: 50
    - path: "src/components/sections/Results.astro"
      provides: "Experimental results cards"
      min_lines: 30
    - path: "src/components/sections/Comparison.astro"
      provides: "Dark-themed comparison table"
      min_lines: 30
    - path: "src/components/sections/Contact.astro"
      provides: "Paper link and contact info"
      min_lines: 10
  key_links:
    - from: "src/components/sections/Invariants.astro"
      to: "src/data/equations.ts"
      via: "import 5 invariant equation constants"
      pattern: "import.*COMPOSITION_ERROR"
    - from: "src/components/sections/Results.astro"
      to: "src/data/results.ts"
      via: "import result data objects"
      pattern: "import.*MNIST_RESULTS"
    - from: "src/components/sections/Comparison.astro"
      to: "src/data/comparisons.ts"
      via: "import comparison table data"
      pattern: "import.*COMPARISON_ROWS"
---

<objective>
Build the bottom four content sections: Invariants, Results, Comparison, and Contact. All copy is pre-written in this plan.

Purpose: Invariants completes the technology explanation (CONT-02). Results presents experimental evidence (CONT-06). Comparison contrasts with existing architectures (CONT-07). Contact provides paper link (CONT-08) and contact info (CONT-09). All copy must pass anti-slop checklist (CONT-10).

Output: Four Astro section components ready for assembly in index.astro.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-content-sections/02-CONTEXT.md
@.planning/phases/02-content-sections/02-RESEARCH.md
@.planning/phases/02-content-sections/02-UI-SPEC.md
@src/components/ui/Section.astro
@src/components/math/Equation.astro
@src/components/math/InlineMath.astro
@src/data/equations.ts
@src/data/results.ts
@src/data/comparisons.ts
</context>

<interfaces>
<!-- Components available -->
From src/components/ui/Section.astro:
```typescript
interface Props { id?: string; class?: string; fullWidth?: boolean; }
```

From src/data/equations.ts (corrected in Plan 01):
```typescript
export const COMPOSITION_ERROR: string;     // Eq 2
export const COMPRESSION_EFFICIENCY: string; // Eq 3
export const SUBALGEBRA_CLEANLINESS: string; // Eq 4
export const PREDICTION_CONSISTENCY: string; // Eq 5
export const ASSOCIATOR_HEALTH: string;      // Eq 6
```

From src/data/results.ts (created in Plan 01):
```typescript
export const MNIST_RESULTS: { headline: { accuracy: 95.2, ... }, knnBaseline: 98.2, gap: -3.0, ... };
export const STABILITY_RESULTS: { overallAccuracy: 97.7, catastrophicForgetting: 0.0, ... };
export const NOVELTY_RESULTS: { spikeRatioDisplay: '5x', ... };
```

From src/data/comparisons.ts (created in Plan 01):
```typescript
export const COMPARISON_ROWS: ComparisonRow[];
export const ARCHITECTURE_NAMES: readonly ['NTM/DNC', 'SDM', 'HTM', 'Oct-Trie'];
```

<!-- Layout contracts from UI-SPEC -->
Results: grid gap-8 sm:grid-cols-2, cards with rounded-lg border border-sigil-dim/30 bg-obsidian p-6
Comparison: overflow-x-auto rounded-lg border, Oct-Trie column with bg-sigil/5 text-sigil border-l
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create Invariants.astro and Results.astro section components</name>
  <files>src/components/sections/Invariants.astro, src/components/sections/Results.astro</files>
  <read_first>
    - src/data/equations.ts (corrected version)
    - src/data/results.ts
    - src/components/ui/Section.astro
    - src/components/math/Equation.astro
    - src/components/math/InlineMath.astro
    - .planning/phases/02-content-sections/02-UI-SPEC.md (Results Section card layout)
    - .planning/phases/02-content-sections/02-CONTEXT.md (D-08, D-09, D-36 through D-39)
    - oct-trie.pdf (pages 8-9 for Section 4: Structural Invariants)
  </read_first>
  <action>
Create both files in src/components/sections/.

---

**Invariants.astro** - Per D-08 (dedicated section with all 5 invariants and equations), D-09 (equations distributed inline).

Imports: Section, Equation, InlineMath, and from equations.ts: COMPOSITION_ERROR, COMPRESSION_EFFICIENCY, SUBALGEBRA_CLEANLINESS, PREDICTION_CONSISTENCY, ASSOCIATOR_HEALTH.

Section heading (h2):
```
Invariants
```

Section intro paragraph:
```
The octonionic trie monitors its own structural health through five invariants derived from the algebra. These invariants replace the role of loss functions in gradient-trained systems. Each produces a scalar signal without labeled data or external supervision.
```

**Invariant 1: Composition error bound**
Paragraph:
```
Sequential octonionic multiplication accumulates floating-point error. The composition error bound tracks this accumulation at each depth level. When the error exceeds a threshold, the node stops accepting new children, establishing a natural resolution limit per branch.
```
Equation: `<Equation latex={COMPOSITION_ERROR} label="2" ariaLabel="Composition error: epsilon of d equals the absolute value of x inverse times the quantity o times x, minus o, divided by the absolute value of o" />`

**Invariant 2: Compression efficiency**
Paragraph:
```
The trie evaluates whether a subtree's structural complexity is justified by its contribution to prediction quality. A high ratio of nodes to effective capacity signals redundancy. Subtrees that consume structure without improving predictions are candidates for consolidation through sibling absorption.
```
Equation: `<Equation latex={COMPRESSION_EFFICIENCY} label="3" ariaLabel="Compression efficiency: rho equals the number of nodes divided by the effective prediction capacity" />`

**Invariant 3: Subalgebra cleanliness**
Paragraph:
```
At each internal node, the pairwise associators between children measure how distinct their occupied subalgebras are. High cleanliness means the branches represent genuinely different algebraic directions. Low cleanliness suggests two children are too similar and should be merged.
```
Equation: `<Equation latex={SUBALGEBRA_CLEANLINESS} label="4" ariaLabel="Subalgebra cleanliness of node n equals the minimum over i not equal to j of the absolute value of the associator of c_i, c_j, and n" />`

**Invariant 4: Prediction consistency**
Paragraph:
```
Before committing an update, the trie estimates how the change would have affected prior predictions stored in the node's memory buffer. The maximum discrepancy across all buffered predictions determines whether the update proceeds. Persistently high discrepancy indicates that the node is attempting to represent fundamentally incompatible information.
```
Equation: `<Equation latex={PREDICTION_CONSISTENCY} label="5" ariaLabel="Prediction consistency: delta of n equals the maximum over y in the buffer of the absolute difference between counterfactual and original predictions" />`

**Invariant 5: Associator health**
Paragraph:
```
The mean associator norm at each node, averaged over recent inputs, measures how well the routing structure accommodates incoming data. A persistently high value signals that the node's children are not organizing data effectively and that restructuring may be needed.
```
Equation: `<Equation latex={ASSOCIATOR_HEALTH} label="6" ariaLabel="Associator health: alpha of n equals one over K times the sum from k equals 1 to K of the absolute value of the associator of x_k, c_k star, and n" />`

Layout:
- Section: `id="invariants" class="border-t border-sigil-dim/20"`
- h2: `font-display text-2xl sm:text-3xl text-sigil mb-12 tracking-wider uppercase`
- Each invariant: a div with h3 (invariant name) + paragraph + equation
- h3: `font-display text-lg text-sigil mb-3`
- paragraphs: `text-chalk leading-relaxed mb-4`

---

**Results.astro** - Per D-36 (summary table/card format), D-37 (MNIST with context, CNN result headline 95.2%), D-38 (stability-plasticity equal prominence), D-39 (skip prerequisite experiments).

Imports: Section, and from results.ts: MNIST_RESULTS, STABILITY_RESULTS.

Section heading (h2):
```
Results
```

Two result cards in a responsive grid.

**Card 1: MNIST Classification**
```
Category label: MNIST CLASSIFICATION
Headline number: 95.2%
Context line 1: CNN encoder, 60,000 training samples
Context line 2: Zero gradient computation in the classifier. Within 3.0 percentage points of k-nearest neighbors on the same features.
```

**Card 2: Stability-Plasticity**
```
Category label: STABILITY-PLASTICITY
Headline number: 0%
Subtext: catastrophic forgetting
Secondary number: 97.7%
Secondary subtext: overall accuracy across 7 sequential categories (342/350 test samples)
```

Layout per UI-SPEC:
- Section: `id="results" class="border-t border-sigil-dim/20"`
- Grid: `grid gap-8 sm:grid-cols-2`
- Cards: `rounded-lg border border-sigil-dim/30 bg-obsidian p-6`
- Category label: `font-display text-sm text-sigil tracking-wider uppercase mb-2`
- Headline number: `font-display text-2xl text-glyph` (using result number size from UI-SPEC)
- Context text: `text-sm text-chalk-muted mt-2`
  </action>
  <verify>
    <automated>cd /home/aescalera/code/github.com/discur-ai/discur.ai && test -f src/components/sections/Invariants.astro && test -f src/components/sections/Results.astro && echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - File src/components/sections/Invariants.astro exists
    - File src/components/sections/Results.astro exists
    - grep "import.*COMPOSITION_ERROR" src/components/sections/Invariants.astro returns a match
    - grep "import.*ASSOCIATOR_HEALTH" src/components/sections/Invariants.astro returns a match
    - grep -c "Equation" src/components/sections/Invariants.astro returns at least 5 (5 equations rendered)
    - grep "Composition error" src/components/sections/Invariants.astro returns a match (invariant 1 name or description)
    - grep "Compression efficiency" src/components/sections/Invariants.astro returns a match
    - grep "Subalgebra cleanliness" src/components/sections/Invariants.astro returns a match
    - grep "Prediction consistency" src/components/sections/Invariants.astro returns a match
    - grep "Associator health" src/components/sections/Invariants.astro returns a match
    - grep "loss functions" src/components/sections/Invariants.astro returns a match (intro paragraph)
    - grep "95.2" src/components/sections/Results.astro returns a match
    - grep "0%" src/components/sections/Results.astro returns a match (forgetting rate)
    - grep "97.7" src/components/sections/Results.astro returns a match
    - grep "catastrophic forgetting" src/components/sections/Results.astro returns a match
    - grep "3.0 percentage points" src/components/sections/Results.astro returns a match (MNIST context per D-37)
    - grep "bg-obsidian" src/components/sections/Results.astro returns a match (card background)
    - grep "grid" src/components/sections/Results.astro returns a match (card grid)
    - Anti-slop: grep -ic "groundbreaking\|revolutionary\|paradigm\|unprecedented" src/components/sections/Invariants.astro returns 0
    - Anti-slop: grep -ic "groundbreaking\|revolutionary\|paradigm\|unprecedented" src/components/sections/Results.astro returns 0
  </acceptance_criteria>
  <done>Invariants.astro presents five invariants each with explanation paragraph and rendered equation. Results.astro shows MNIST (95.2%) and stability-plasticity (0% forgetting, 97.7%) in card layout with proper context. All copy is anti-slop clean.</done>
</task>

<task type="auto">
  <name>Task 2: Create Comparison.astro and Contact.astro section components</name>
  <files>src/components/sections/Comparison.astro, src/components/sections/Contact.astro</files>
  <read_first>
    - src/data/comparisons.ts
    - src/components/ui/Section.astro
    - .planning/phases/02-content-sections/02-UI-SPEC.md (Comparison Table layout, Contact section)
    - .planning/phases/02-content-sections/02-CONTEXT.md (D-10, D-11, D-12, D-40)
    - oct-trie.pdf (page 10 for Table 1)
  </read_first>
  <action>
Create both files in src/components/sections/.

---

**Comparison.astro** - Per D-10 (3 rows, 4 columns, Oct-Trie highlighted gold), D-40 (dark themed HTML, gold accent).

Imports: Section, and from comparisons.ts: COMPARISON_ROWS, ARCHITECTURE_NAMES.

Section heading (h2):
```
Comparison
```

Intro paragraph:
```
The octonionic trie shares individual features with existing memory-augmented architectures but combines them through a single algebraic substrate rather than independent engineered components.
```

Table structure per UI-SPEC:
- Container: `overflow-x-auto rounded-lg border border-sigil-dim/30`
- Table: `w-full text-left text-sm`
- Header row: `border-b border-sigil-dim/30 bg-obsidian`
- Header cells (first 4): `px-4 py-3 font-display text-sm text-chalk-muted`
- Oct-Trie header: `px-4 py-3 font-display text-sm text-sigil border-l border-sigil-dim/30 bg-sigil/5`
- Body: `divide-y divide-sigil-dim/20`
- Body cells (first 4): `px-4 py-3 text-sm text-chalk`
- Oct-Trie body cells: `px-4 py-3 text-sm text-sigil border-l border-sigil-dim/30 bg-sigil/5`
- First column (property name): `px-4 py-3 text-sm text-chalk-muted font-display`

Render the table dynamically from COMPARISON_ROWS data. The header row has columns: (empty/Property), NTM/DNC, SDM, HTM, Oct-Trie. Each body row maps from the ComparisonRow interface.

---

**Contact.astro** - Per D-11 (paper link to GitHub), D-12 (research@discur.ai + github.com/discur-ai).

Imports: Section.

Section heading (h2):
```
Paper
```

(Using "Paper" as the single-word heading per D-24, since this section primarily links to the paper.)

Paragraph:
```
The full technical treatment, including proofs, experimental methodology, and open questions, is available on GitHub.
```

Link:
```html
<a href="https://github.com/discur-ai" class="inline-flex items-center text-sigil hover:text-glyph transition-colors font-display tracking-wider">
  Read the paper
  <svg class="ml-2 w-4 h-4" viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="1.5">
    <path d="M6 3l5 5-5 5" />
  </svg>
</a>
```

Contact info below the paper link:
```html
<p class="mt-8 text-sm text-chalk-muted">
  <a href="mailto:research@discur.ai" class="hover:text-sigil transition-colors">research@discur.ai</a>
  <span class="mx-3">·</span>
  <a href="https://github.com/discur-ai" class="hover:text-sigil transition-colors">github.com/discur-ai</a>
</p>
```

Layout:
- Section: `id="contact" class="border-t border-sigil-dim/20"`
- Centered text alignment is optional (per discretion). Left-aligned is fine.
  </action>
  <verify>
    <automated>cd /home/aescalera/code/github.com/discur-ai/discur.ai && test -f src/components/sections/Comparison.astro && test -f src/components/sections/Contact.astro && echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - File src/components/sections/Comparison.astro exists
    - File src/components/sections/Contact.astro exists
    - grep "import.*COMPARISON_ROWS" src/components/sections/Comparison.astro returns a match
    - grep "overflow-x-auto" src/components/sections/Comparison.astro returns a match (mobile scroll)
    - grep "bg-sigil/5" src/components/sections/Comparison.astro returns a match (Oct-Trie column highlight)
    - grep "border-l" src/components/sections/Comparison.astro returns a match (Oct-Trie column border)
    - grep "bg-obsidian" src/components/sections/Comparison.astro returns a match (header background)
    - grep "NTM/DNC" src/components/sections/Comparison.astro returns a match (architecture name in header or data)
    - grep "single algebraic substrate" src/components/sections/Comparison.astro returns a match (intro paragraph)
    - grep "research@discur.ai" src/components/sections/Contact.astro returns a match
    - grep "github.com/discur-ai" src/components/sections/Contact.astro returns a match
    - grep "Read the paper" src/components/sections/Contact.astro returns a match
    - grep "border-t border-sigil-dim" src/components/sections/Comparison.astro returns a match
    - grep "border-t border-sigil-dim" src/components/sections/Contact.astro returns a match
    - Anti-slop: grep -ic "groundbreaking\|revolutionary\|paradigm\|unprecedented\|seamless" src/components/sections/Comparison.astro returns 0
    - Anti-slop: grep -ic "delve\|utilize\|leverage\|harness" src/components/sections/Contact.astro returns 0
  </acceptance_criteria>
  <done>Comparison.astro renders a dark-themed 3-row comparison table with gold-highlighted Oct-Trie column. Contact.astro provides paper link to GitHub and research@discur.ai contact. All copy is anti-slop clean.</done>
</task>

</tasks>

<verification>
- Invariants section has all 5 invariants with equations (D-08)
- Results section shows 95.2% MNIST and 0% forgetting with context (D-36, D-37, D-38)
- Prerequisite experiments skipped (D-39)
- Comparison table has exactly 3 rows (D-10)
- Oct-Trie column highlighted with gold accent (D-40)
- Paper links to GitHub (D-11)
- Contact shows research@discur.ai (D-12)
- No companion paper mentioned (D-13)
- No author name (D-14)
- No epistemic honesty notes (D-15)
- All copy passes anti-slop checklist (D-17)
- `npx astro check` passes
</verification>

<success_criteria>
- Four section components exist and import their data dependencies correctly
- All pre-written copy appears verbatim in the components
- Comparison table renders 3 rows x 4 columns with proper dark styling
- Results cards show specific numbers with context
- Contact section has working links to email and GitHub
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-sections/02-04-SUMMARY.md`
</output>
